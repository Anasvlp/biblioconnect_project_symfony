{% extends 'base.html.twig' %}

{% block title %}Détail du commentaire{% endblock %}

{% block body %}
<div style="max-width: 800px; margin: 2em auto;">
    <h1>Commentaire</h1>

    <p><strong>Utilisateur :</strong> {{ comment.reservedBy }}</p>
    <p><strong>Livre :</strong> {{ comment.book }}</p>
    <p><strong>Contenu :</strong> {{ comment.content }}</p>
    <p><strong>Note :</strong> {{ comment.rating }}</p>

    <div style="margin-top: 2em;">
        <a href="{{ path('app_comment_edit', {'id': comment.id}) }}">Modifier</a> |
        <a href="{{ path('app_comment_index') }}">Retour</a>
    </div>

    {{ include('comment/_delete_form.html.twig') }}
</div>
{% endblock %}
